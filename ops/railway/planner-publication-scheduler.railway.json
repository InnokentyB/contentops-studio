{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "npm ci && npm run build:backend"
  },
  "deploy": {
    "startCommand": "npm run scheduler:publication",
    "cronSchedule": "0 * * * *",
    "restartPolicyType": "NEVER"
  }
}
